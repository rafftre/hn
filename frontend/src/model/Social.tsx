class Social {
  id: string;
  url: string;
  icon: string;

  constructor(
    id: string,
    url: string,
    icon: string
  ) {
    this.id = id;
    this.url = url;
    this.icon = icon;
  }
}

export default Social;
